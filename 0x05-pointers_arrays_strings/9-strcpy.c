#include "main.h"
#include <string.h>
/**
 **_strcpy - Copies the string
 *@src: pointer
 *@dest: pointer
 *Return: return pointer
 */
char *_strcpy(char *dest, char *src)
{
int i, n;
for (i = 0; src[i] != '\0'; i++)
{
}
for (n = 0; n <= i; n++)
{
dest[i] = src[i];
}
return (dest);
}
