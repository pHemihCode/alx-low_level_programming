#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_strlen - Function to return the length
 *@a: variable to store the length
 *Return: Always 0
 */
int _strlen(char *s)
{
int a;
a = strlen(s);
return (a);
}
